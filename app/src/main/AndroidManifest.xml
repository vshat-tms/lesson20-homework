<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lessson17">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lessson17">
        <activity
            android:name=".OtherApplications"
            android:exported="false" />
        <activity
            android:name=".EditCounter"
            android:exported="false"
            android:label="@string/edit_counter" />
        <activity
            android:name=".CreateCounter"
            android:exported="false"
            android:label="@string/create_counter" />
        <activity
            android:name=".ImageActivity"
            android:exported="false"
            android:label="@string/image_label" />
        <activity
            android:name=".InfoActivity"
            android:exported="false"
            android:label="@string/info_label" />
        <activity
            android:name=".CounterActivity"
            android:exported="true"
            android:label="@string/counter_label" />
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:label="@string/menu_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>