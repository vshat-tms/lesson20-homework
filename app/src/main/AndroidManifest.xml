<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lessson17">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lessson17">
        <activity
            android:name=".ShareActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SetCounterActivity"
            android:label="SetCounterActivity"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:label="@string/menu_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateCounterActivity"
            android:exported="false"
            android:label="CreateCounterActivity" />
        <activity
            android:name=".InfoActivity"
            android:exported="false"
            android:label="@string/info_activity" />
        <activity
            android:name=".ImageActivity"
            android:exported="false"
            android:label="@string/image_activity" />
        <activity
            android:name=".CounterActivity"
            android:exported="false"
            android:label="@string/counter_activity"></activity>
    </application>

</manifest>